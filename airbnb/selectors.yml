selectors:
    - name: dim_except_listings_with_hosts
      description: Selects all dim models. excluding dim_listings_with_hosts
      definition:
        union:
          - method: path
            value: models/dim
          - exclude:
            - method: fqn
              value: dim_listings_with_hosts
